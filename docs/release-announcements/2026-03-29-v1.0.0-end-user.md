# CodexSubMcp v1.0.0 已发布

CodexSubMcp 1.0.0 现已发布。

这次版本把 Windows GUI 进一步简化到最核心、最安全的清理路径：

- 先刷新
- 再校验父进程识别是否可信
- 只对 orphan MCP 目标执行清理

你会看到更简单的总览页，以及更少的导航层级；MCP 检索结果已经并回总览展示。

如果你是在多父会话、多子代理并行的场景下使用，这个版本更适合长期挂着做日常巡检。
