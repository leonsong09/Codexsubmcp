# v0.2.1 - Packaging Hotfix

本版本是 `v0.2.0` 的发布热修，目标是把 Windows `exe` 打包和源码安装链路补齐到可重复发布状态。

## 主要更新

- 修复 `pip install -e .[dev]` 的 setuptools 自动包发现问题
- 显式限制仅打包 `codexsubmcp*` 包，排除 `packaging*` 和 `temp*`
- 新增打包元数据回归测试，避免后续再次把发布链路改坏
- 重新构建 `CodexSubMcpManager.exe` 并生成 `SHA256` 校验文件

## 验证

- `venv\Scripts\python.exe -m pytest tests/test_packaging_metadata.py -q`
- `QT_QPA_PLATFORM=offscreen venv\Scripts\python.exe -m pytest tests/test_gui_smoke.py -q`
- `QT_QPA_PLATFORM=offscreen venv\Scripts\python.exe -m pytest -q`
- `venv\Scripts\pyinstaller.exe packaging/windows/CodexSubMcpManager.spec --noconfirm`
- `dist\CodexSubMcpManager.exe config validate --config tools\codex_mcp_watchdog.example.json`

## 发布产物

- `CodexSubMcpManager.exe`
- `checksums.txt`

## 已知说明

- PyInstaller warning 文件中仍会出现若干 Windows 下无关的 POSIX 可选模块缺失记录，这不影响当前 GUI 发布
